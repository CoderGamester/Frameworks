# Architecture Log Archive

Entries pruned from `.architecture-log.md` during periodic maintenance. This file is **gitignored** and is never read automatically — it exists only as a manual-request historical reference.

## Pruned 2026-04-21

### No-op entries removed (3 tooling-only sessions with no architectural changes)

#### 2026-03-13 — AI tooling consolidation: skills, commands, transcript mining

No architectural changes to the Frameworks project this session. Work was entirely personal tooling: consolidated the Cursor/Claude Code skill and command ecosystem — inlined wrapup spec into `conversation-wrapup` skill, restructured transcript mining into per-project and cross-project commands, renamed commands for naming consistency, and identified the `continual-learning` plugin stop hook for uninstall.

#### 2026-03-12 — Wrapup skill integrated with unity-tests-audit

No architectural changes to the Frameworks project this session. Work was entirely tooling: integrated test coverage analysis into the wrapup flow (session-scoped static Phase 1 + deferred Phase 2 execution offer), created a standalone `/audit-tests` command for full-project audits, and refined the approval UX across all wrapup/mine-transcripts commands (dropped `Edit` keyword, added `[User/Project/Skip]` for skill location).

#### 2026-03-12 — Personal skill: unity-tests-audit

No architectural changes to the Frameworks project this session. Work was entirely tooling: created the personal `unity-tests-audit` skill at `~/.claude/skills/unity-tests-audit/` for auditing and running Unity NUnit test suites across any project type (game project, host repo, standalone package).

## Pruned 2026-05-12

### No-op entries removed (1 tests-only audit session)

#### 2026-05-04 — GameData test audit (Thorough mode, +15 stubs) — surfaced two design-smell follow-ups

No architectural changes this session. Audit was tests-only (`com.gamelovers.gamedata` v1.0.2 → v1.0.3), all 15 written stubs landed under `Tests/Editor/Unit/`. Two design smells were surfaced for future patches but not applied: (1) `Runtime/Serialization/ValueData.cs:171` `Vector2Serializable.op_Implicit → Vector2` returns `new Vector3(...)` and should be `new Vector2(...)` (functionally equivalent today, misleading); (2) `Editor/Elements/DependencyGraphElement.cs:46` reads `ComputedField<T>._dependencies` via reflection and should consume an `internal IReadOnlyCollection<IComputedDependency> Dependencies` seam on `ComputedField<T>`.
